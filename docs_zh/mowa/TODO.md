# MoWA TODO

- 补 `E-001` long-training command draft / launch entrypoint，并把 baseline / MoWA 两套 4090 profile（`bs=1`、`ga=32`、`warmup=500`）接到正式启动前 preview。
- 明确 `class_mapping_status` 从 `Data Gate` 到确认态的收口条件，并同步到 readiness / matrix。
- 使用更强 checkpoint 重新执行 `E-006` rollout，对 bridge 干预拿到有信息量的对比证据。
